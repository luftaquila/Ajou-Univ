library ieee;                                 -- IEEE ????? ??
use ieee.std_logic_1164.all;
use ieee.std_logic_arith.all;
use ieee.numeric_std.all;

entity toParity is                              -- entity ??
  port(                                       -- ?? ?? 9?, ?? ?? 2? ??.
    I0, I1, I2, I3, I4, I5, I6, I7, I8 : in std_logic;
    Q0, Q1 : out std_logic
  );
end toParity;

architecture run of toParity is                 -- toParity ?? ???? ??
  begin                                       -- ??? ?? ?? ?? ??
    Q0 <= I0 XOR I1 XOR I2 XOR I3 XOR I4 XOR I5 XOR I6 XOR I7 XOR I8;    -- ?? ??? ?? : ? ??? ?? XOR ??? ???? Q0? ??
    Q1 <= NOT Q0;                           -- ?? ??? ?? : ? ??? ?? ?? ???? ?? ???? ?? ???? ?? ???? NOT ??? ???? Q1? ??.
end architecture run;
